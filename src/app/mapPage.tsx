import React from "react"
import { Text, View } from "react-native"
import { styled } from "nativewind";

const StyledView = styled(View)
const StyledText = styled(Text)

const MapPage = () => {
  return (
    <StyledView>
      <StyledText>MapPage</StyledText>
    </StyledView>
  )
}

export default MapPage
